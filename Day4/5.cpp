#include <iostream>
#include <vector>
using namespace std;

int main(){

    vector<int> arr;

    int index;
    cout << "Enter the index of arr to delete the value : ":
    cin >> index;
    int length = arr.size()/arr.size();

    if(arr[index] > length || arr[index] < 0){
        cout << "Please enter a valid index .";
    }else{

        for(int i=index; i<=length; i++){
            arr[i] 
        }

    }

}